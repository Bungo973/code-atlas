# Code Atlas

把一个代码仓库解析成可交互的依赖关系图，帮人搞清楚「这个项目是怎么连起来的」。
全程在浏览器本地完成。

本文件只是**词汇表**。设计决策见 [`docs/DECISIONS.md`](./docs/DECISIONS.md)，
界面区域命名见 [`docs/UI-VOCABULARY.md`](./docs/UI-VOCABULARY.md)，
计划与验收标准见 [`docs/PLAN.md`](./docs/PLAN.md)。

## 语言

### 图的构成

**文件**：
被扫描并解析的一个源码文件，图上的一个节点。id 是相对仓库根的 posix 路径。
_Avoid_: 模块、node（画布上下文里说「节点」可以，指的是同一个东西）

**依赖**：
两个文件之间「A 引用了 B」的关系，图上的一条边。同一对文件之间只有一条，
无论 A 用几条 import 语句引用 B。
_Avoid_: 引用（留给符号级）、link、import 关系

**引用**：
符号级的使用关系：「A 用了 B 导出的 `format`」。比依赖细一层。
_Avoid_: 调用（那是 L3，本项目不做）、依赖

**导出符号**：
一个文件对外暴露的具名导出或默认导出。id 形如 `src/utils.ts#format`。
_Avoid_: API、接口、符号（单说「符号」太泛）

### 分析层次

**L1 文件级**：
节点是文件，边是依赖关系。

**L2 符号级**：
在 L1 之上，把边细化到「用了哪个导出符号」，并据此推断疑似死代码。

**L3 调用图**：
函数之间的调用关系。**本项目明确不做**，理由见 ADR-003 与 ADR-004。
提到 L3 时永远指这件没做的事，不要用它泛指 L2。

### 解析

**specifier**：
import 语句里那个原始字符串，如 `'./utils'`、`'@/components/x'`、`'react'`。
_Avoid_: 路径（它未必是路径）、模块名

**解析**：
把 specifier 翻译成硬盘上真实存在的那个文件。这是本项目的核心动作。
_Avoid_: 查找、resolve 路径

**别名作用域**：
一份 tsconfig/jsconfig 的 `paths` 所覆盖的目录子树。一个仓库可以有多份，
每个文件适用**最近祖先**的那一份。
_Avoid_: 别名配置、tsconfig（指配置文件本身时才叫 tsconfig）

**命中率**：
`成功解析 ÷ (成功解析 + 真实失败)`。**外部包、静态资源、设计上无法解析的三类都不进分母。**
这个指标有已知盲区：被误分类的 import 不会进分母，漏提取的 import 更是根本不出现——
见 ADR-006、ADR-012。
_Avoid_: 准确率、成功率

**外部包**：
解析结果指向仓库之外的 npm 包，不画进图。
_Avoid_: 第三方依赖、node_modules 依赖

**设计上无法解析**：
构建产物（`./dist/...`）、框架虚拟模块（SvelteKit 的 `./$types`）、超出所选根目录三类。
它们**不是解析器的缺陷**，因此不计入失败率。
_Avoid_: 失败、错误

**疑似漏配别名**：
长得像路径别名（`@/`、`~/`、`#` 开头）却没匹配上任何别名作用域的裸 specifier。
数量大于 0 通常意味着**图缺边**。
_Avoid_: 未解析的别名

### 图指标

**直接入度**：
有多少个文件**直接** import 它。决定节点在画布上的大小。
_Avoid_: 入度（必须说清是直接还是传递）、被引用次数

**出度**：
它自己 import 了几个仓库内的文件。和直接入度一起构成一个节点的局部形状。
_Avoid_: 依赖数、引用数

**传递入度**：
有多少个文件直接或间接依赖它。**在强连通的大仓库里会饱和**（excalidraw 里几乎每个文件都是 509）。
因此有三条硬规矩（ADR-019、ADR-021）：不做排序键、不做视觉编码、不单独出现。
_Avoid_: 入度、重要性、影响力

**枢纽**：
**直接**入度高的文件。改动它波及面大。
_Avoid_: 核心文件、热点

**入口点**：
没有任何文件依赖它、但它依赖别人的文件。适合作为阅读起点。
_Avoid_: 根节点、main

**孤岛**：
既不依赖别人也不被依赖的文件。
_Avoid_: 孤立节点、死文件

**循环依赖**：
一组互相可达的文件（强连通分量，size > 1，或自环）。
_Avoid_: 环形引用、循环引用

**影响范围**：
改动某个文件后，沿反向依赖可达的文件，**永远带一个层级上限**。
不限层级时在强连通的大仓库里会饱和（excalidraw 上点哪个文件都是 509 个），
所以说「影响范围」必须同时说清是几跳——见 ADR-020。
_Avoid_: 依赖方、下游（方向容易说反）、blast radius

**跳数**：
某个文件到选中文件的最短反向路径长度。1 = 直接 import 选中文件。
_Avoid_: 层级、距离、深度（「深度」留给目录树）

**传播步**：
参与影响传播的一条边：`跳数(依赖方) = 跳数(被依赖方) + 1`。
同层之间的横向边不算传播步，画布上不高亮。
_Avoid_: 路径、链路

**疑似死代码**：
在仓库内找不到任何引用的导出符号。**「疑似」两个字不可省略**——
可能被动态引用、被仓库外的消费方引用，或本身就是公开 API。
_Avoid_: **未使用**（禁止：把猜测说成结论）、无用代码、僵尸代码

**豁免**：
从疑似死代码里排除掉的三类：有引用、所属文件被命名空间导入（`import * as`）、
所属文件是入口点。第二类占比最大，缺了它误报率会从 7% 涨到 35%。
_Avoid_: 白名单、忽略
